int search(int* nums, int numsSize, int target) {
    int flag=-1;
    for(int i=0;i<numsSize;i++)
    {
        if(nums[i]==target)
        {
            flag=i;
            break;
        }
    }
    return flag;
}